---
driver:
  name: vagrant

provisioner:
  name: chef_infra
  product_name: chef
  enforce_idempotency: true
  multiple_converge: 2
  deprecations_as_errors: true
  data_bags_path: test/fixtures/data_bags

verifier:
  name: inspec

platforms:
  - name: centos-7
  - name: debian-11
  - name: ubuntu-18.04

suites:
  - name: client
    run_list:
      - recipe[ossec::client]
    data_bags_path: 'test/fixtures/data_bags'
  - name: server
    run_list:
      - recipe[ossec::server]
    data_bags_path: 'test/fixtures/data_bags'
